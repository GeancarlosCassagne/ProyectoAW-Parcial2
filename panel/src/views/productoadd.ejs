<% layout('layouts/main') -%>
    <section class="contenedor wrapper">
        <div id="main">
            <fieldset>
                <legend>DETALLES DEL PRODUCTO</legend>
                <form action="/api/panel/productos/add" method="post">
                    <label>Nombre</label>
                    <input type="text" name="nombre" placeholder="Descripcion" required>
                    <label>Precio</label>
                    <input type="text" name="precio" placeholder="0.00" required>
                    <label>Categoria</label>
                    <select name="_categoria" id="">
                        <% if (data.length > 0) { %>
                            <% for (let i=0; i<data.length; i++) { %>
                                <option value="<%= data[i].id %>"><%= data[i].descripcion %></option>
                                <% } %>
                                    <% } %>
                    </select>
                    <label>Descripcion</label>
                    <textarea name="descripcion"></textarea>
                    <label>Foto</label>
                    <input type="text" name="foto" required>
                    <button type="submit" name="accion" value="Añadir" class="boton boton-verde enviar">Agregar</button>
                    <button class="boton boton-rojo" href="/api/panel/productos">Cancelar</button>
                </form>
            </fieldset>
        </div>
    </section>